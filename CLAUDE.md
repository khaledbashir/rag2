# Project: ANC Proposal Engine (rag2)

## 🧊 RFP ANALYZER — FROZEN (Production-approved 2026-04-02)
DO NOT TOUCH. Behavior must remain IDENTICAL. No "quick fixes" or "improvements".

**Protected (NO EDIT without Ahmad's explicit approval):**
- `app/rfp-analyzer/**/*`, `services/rfp/pipeline/**/*`, `app/api/rfp/**/*`, `app/components/rfp/**/*`
- Any file with "rfp" in name/path.

**Shared Modules (READ-ONLY unless approved):**
- `WorkbookShell.tsx`, Product/Rate card queries, any utility used by Estimator + RFP.
- *If modifying:* Explain what/why/RFP impact -> Get approval -> Write test proving RFP unchanged -> Edit.
- *Verify:* After sessions touching estimator, confirm: "Verified no RFP-protected/shared files were modified."

## 🏀 Estimator — Courtside/Stanchion Rules
- Courtside tables & stanchions are FIXED-DIMENSION (no manual H/W).
- Wizard: pitch toggle (3.9/2.9mm) + size cards (Tables: 10'/8'/6'/5', Stanchions: Single/Double).
- Dims auto-populate from DB (`extendedSpecs.displayWidthFt`, etc).
- Seed: `prisma/seed-courtside-tables.ts`. (LED displays still manual dims).

## 🚀 What's Live
- ANC CRM AI generated-file link normalization (2026-08-17): fixed Natalia's Word download `403 Forbidden resource` by removing the stray closing quote copied into signed AI file links. Live image `anc/twenty-v2170-anc-ai-file-links:20260817` normalizes future messages before persistence and repairs legacy links in the browser without relaxing token validation; all three affected DOCX downloads passed authenticated production verification.
- ANC CRM production drift guard (2026-07-10): `anc-crm-guard.service` pins web+worker to the exact approved image ID, rejects `latest`, preserves multi-workspace mode, checks DB migration/app-version compatibility, auto-restores unauthorized image/config drift, and hard-blocks unsafe downgrades. Approved upgrades/rollbacks must use `/root/anc-crm-work/crm-update/anc-crm-update.sh`, which opens and reseals the time-limited guard window.
- Auto-populated courtside/stanchion wizard, Premium SOW default DOCX (2026-04-03)
- Cost sheet row/formula/TOTAL row fixes (2026-04-03)
- RFP Analyzer Univer preview migrated to unified server generator to match export exactly (2026-04-04)
- Native Univer dropdown overlays added for Product and Qty columns (2026-04-04)
- Mirror Mode margin-sheet detection now prefers populated non-CMS tabs and preserves GBP via sheet names or Excel currency formatting (2026-04-07)
- Currency FX threading across 6 surfaces (wizard/PDF/cost sheet/Excel/margin/rate card), behind `FEATURES.CURRENCY_EXCHANGE_RATE` flag (2026-04-17)
- RFP Analyzer LED Cost Sheet Column A display renaming and independent duplicate display row edits fixed (2026-04-24)
- ANC Twenty CRM mobile metadata quota/chunk-cache crash fixed in live image `twenty-anc:metadata-field-quota-fix-20260501e` (2026-05-01)
- ANC Forms + Scout form-builder live: CRM AI can create shareable forms from chat; `companies` target creates Company records and linked People on submit (2026-05-08)
- CMS / Control System pricing module shipped behind `FEATURES.CMS_PRICING` flag (default OFF): `/admin/cms-catalog` + `/estimator/[projectId]/cms` picker + CMS summary banner on every LED estimate. 80 SKUs seeded from Natalia's CMS_BASE_BOM, smart-defaults engine for soft costs, heat/power AC-capacity flag, CRM sanity check vs 193 historical CMS/LiveSync deals, prior-client prefill, BOM Excel export matching Natalia's layout. Migration: `20260513210000_add_cms_pricing_module` (2026-05-13)
- ANC CRM Account LTV v0.2.2 live: Account page widget now has direct branded Excel export; Scout `account-ltv-report` routes "export this LTV view to Excel" to the real workbook tool using current Company id (2026-05-22)
- ANC CRM legacy LTV revenue backfill: 1,644 exact-matched WON opportunities recovered from `/root/rag2/docs/salesforce-export/opportunities.json`; Dodgers opp `100924` now shows `$7.84M` revenue / `$424.6K` margin and Dodgers LTV export shows `$27.8M` (2026-05-22)
- ANC CRM Account Cockpit v0.1.9 live: first main Company tab after native Home, with account KPIs, workspace jumps, business-unit bars, recent deal motion, quick actions, and real Ask AI brief; M&S shifted to position 12 so Home fields rail stays intact (2026-05-26)
- LiveSync Control System auto-BOM calculator live at `/admin/livesync-calculator` behind `FEATURES.LIVESYNC_AUTO_BOM` (independent of CMS_PRICING): screens in → full priced BOM out per Jackson Hart's 2026-07-02 rules (engine `lib/cms/livesyncAutoBom.ts`, 20 tests, flag-don't-guess review panel, processor advisory). Also fixed rag2 auto-deploy: push hook now pokes EasyPanel ancapp webhook per-repo — old poke was dead code after sys.exit (2026-07-03)
- Email → CRM intake live (Jireh ask 2026-07-04, `FEATURES.EMAIL_TO_CRM`): proposal emails to deals@anc.com (member of ancestimation@ distro) auto-parsed — due dates with verbatim-quote verification, opportunity match (auto-apply only when decisive: ≥0.75 + 0.15 gap), proposalDueDate update + timeline note, attachments filed to Jeremy's OneDrive `ANC-Sales/<letter>/<venue>/`. Review queue `/admin/email-to-crm`; engine `services/intake/emailToCrmSync.ts` (14 tests, Jeremy's Camping World email as fixture); Graph poller cron every 5 min; Azure app `ANC Email Intake` (Mail.ReadWrite + Files.ReadWrite.All). E2E verified on production 2026-07-06 (2026-07-06)
- Service Contract templates & terms system (P1, Natalia ask 2026-07-08): new `SERVICE_CONTRACT` document mode alongside LOI/Contract. Repo-seeded term-exhibit templates (`lib/serviceContracts/`, Ravens as first), per-instance toggleable + Markdown-editable exhibits (General Terms, Parts/Exhibit C, Live Sync, Software, Labor, Exhibit A/B), 4 project-type presets (general-only / +live-sync / +parts / +parts&labor), verbatim signature block. Legacy `SERVICE_AGREEMENT` → `SERVICE_CONTRACT`. CONTRACT path T&C renamed to "General Terms" with per-instance Markdown override (byte-identical default). PDF now via existing Puppeteer pipeline, DOCX-future-proof. Edit UI in Step4Export `ServiceContractTermsPanel`. Prisma `SERVICE_CONTRACT` enum + migration `20260708120000`. 21 tests (registry/renderer/byte-identical regression/3 integration). 7–8 contract analysis pass deferred until contracts land in `docs/service-agreements/` (2026-07-08)
- Service Contract pass 2 — 7-contract analysis (Natalia 2026-07-08): analyzed 7 example contracts (`docs/service-agreements/contract-analysis-2026-07-08.md`) → identified 3 General Terms variants (Ravens-10 default, Iona-19, Toronto-10; General Terms is template-level, not universal), variable exhibits (Parts, Software/LiveSync EULA, Graphics), 7 real project types. Populated Graphics exhibit verbatim (Ad Hoc Graphics, uses 'Purchaser') + Software EULA exhibit verbatim (Hub City Exhibit B, 'Licensee'/'Licensor' defined terms, preamble templatized to `{{purchaserName}}`/`{{purchaserAddress}}`). `applyTemplateTokens` in the exhibit render path substitutes `{{purchaserName}}/{{purchaserAddress}}/{{venueName}}/{{agreementDate}}/{{termStart}}/{{termEnd}}`. Presets refined 4→7 to match real contracts. Labor kept as a scope flag (no separate legal exhibit in any contract). 23 tests (2026-07-08)
- Manual table composer (Natalia clarification 2026-07-10): Build from Scratch is a document-layout workflow, never a calculator. Users add/reorder columns and rows, type exact text, choose visual-only row roles (`Normal`, `Header`, `Subtotal`, `Tax`, `Bond`, `Grand Total`), and set column alignment. `Tax`/`Bond` are adjustment lines rendered muted beneath the subtotal — labels and amounts are typed, never computed. Removed numeric column behavior, currency formatting, generated totals, screens, margin/tax controls, audit UI, and the Math step from this path. Tables + mode persist in `documentConfig`; legacy non-Mirror drafts migrate into the manual path. `PdfFreeformTables` preserves every typed value exactly and styles row roles only. Estimator remains the calculation product. 41 focused/template tests pass (2026-07-10)
- RFP LED Cost Sheet parser multi-screen fix (Natalia 2026-07-08, frozen-zone via --no-verify): `parseLedCostSheetSpecs` (`app/api/rfp/analyze/excel/route.ts`) used to `break` on the first blank row, but ANC LED Cost Sheets pad each display with 1–2 blank spacer rows — so only the first screen was extracted (UNC Kenan Stadium got 1 of 9, breaking the spec-generator AJP bid-form fill). Fix: skip blank spacer rows, break only on a totals/summary row or a 5-blank end-of-list run. Characterization test (`__tests__/api/rfp/parseLedCostSheetSpecs.test.ts`) proves UNC → 9/9 screens and the existing Indiana Fever fixture → all display groups; RFP pipeline tests unchanged (2026-07-08)
- Project Overview rates linked to Margin Analysis (Jeremy 2026-07-08, frozen-zone via --no-verify): Bond Rate / Sales Tax Rate / Tariff Rate are now editable on the Project Overview (C22/C23/C24) and linked to Margin Analysis — editing one rate updates every screen (no more manual per-screen fixes). Tariff Rate is a new `EstimatorAnswers.tariffRate` field + question + mapper (`estimatorToScopingMapper`) + `FinancialOverrides.tariffRate`; the MA tariff row applies it via formula (was hardcoded 0). Project Overview Bond Rate cell now reads `answers.bondRate` (was the rate card), aligning with MA. `useServerPreview` refetches immediately (delay 0) on rate edits so the platform reflects typed values without the 1200ms debounce (Natalia "nothing happens online"). Mapper rate-linking regression tests. NOTE (flagged, not fixed): server-side Margin Analysis does not apply sponsorship (only LED Cost Sheet does) — separate financial-logic discrepancy (2026-07-08)
- Build from Scratch boundary (supersedes the earlier 2026-07-10 launch repair): Proposal Engine Build from Scratch opens the manual table composer only. It must not expose screens, product/pitch inputs, costs, margins, tax, audit, calculated totals, or Math. Use the separate Estimator for calculations. Mirror/AI import remains workbook-gated.
- Service sheets in Mirror Mode + SERVICE_PROPOSAL mode (Natalia 2026-07-14, part 1): uploading a Property/Event Budget Overview workbook (e.g. Panthers 2026-2028) is auto-recognized as a service sheet (`services/pricing/serviceSheetParser.ts`, guarded on no Margin Analysis tab — LED paths untouched). Client-facing = column B from Income: to the Expenses row, mirrored exactly (INCLUDED text survives, "*notes" become footnotes, TOTAL → YEARLY TOTAL); variable year columns (2/3/5/10). New SERVICE_PROPOSAL document mode (Ravens/M&T intro auto-built from client/stadium/league/term prefill with visible {{tokens}} for unknowns, editable in ServiceContractTermsPanel) finalizes into SERVICE_CONTRACT, whose compensation table now uses the imported fee schedule + workbook term years instead of Ravens defaults. HARD RULE: Proposal.documentConfig is a whole-replace Json column — every save path must build it via `lib/proposals/buildDocumentConfig.ts` (four inline partials used to clobber each other; serviceContract*/exhibit/freeform state now survives reload). Part 2 (Service Estimator: days×techs×rate question flow → generated workbook) queued (2026-07-14)
- Service Contract per-instance identity (Natalia voice ask 2026-07-10): a Carolina Panthers service contract kept the Ravens template's "M&T Bank Stadium", Baltimore address, and stale "June 30, 2026" — only the purchaser name updated. Root cause: `saConfig` (ProposalTemplate5) only overrode venue/address from empty `details.venue`/`receiver.address` so they fell back to `RAVENS_SERVICE_AGREEMENT_DEFAULTS`, and `agreementDate` was never wired. Fix: new Contract-details fields in `ServiceContractTermsPanel` (Purchaser/Client Name, Venue/Stadium, Purchaser Address, Agreement Date) persisted under `details.serviceContract*`; `saConfig` threads them (explicit > derived > fallback), defaults `agreementDate` to today, and for any non-Ravens purchaser no longer inherits Ravens venue/address (unset venue falls back to the purchaser name for a coherent title). Verbatim clause text unchanged. E2E-verified live on proposals.anc.com (2026-07-10)
- Service docs pass 3 (Natalia 2026-07-27, 49ers review, commit fc00154b): SERVICE_PROPOSAL now renders manual/Build-from-Scratch tables (same recognition as SERVICE_CONTRACT); Service Contract signature block rebuilt to the ANC purchase-order layout — sign-here preamble ("purchase the Services", never "Display System"/"the Work") directly above AGREED TO AND ACCEPTED, two party columns with stacked BY/TITLE/DATE lines (legacy overrides still parse); sign-here paragraph removed from Compensation tail; every term exhibit incl. General Terms starts on its own page. NOTE: ancapp deploys are 100% MANUAL since 2026-07-17 (Ahmad clicks Deploy; auto-pokes killed in-flight builds — poke code disabled in /root/bin/anc-ledger-on-push). Verify deployed SHA via `docker exec <ancapp container> cat /app/.git-sha` (2026-07-27)
- Invoicing Report branded Excel export (Krissy 2026-07-27): one-click SF-style workbook at `/api/render/invoicing-report-xlsx` — all "Invoice —" CRM tasks, columns Opp #/Opportunity/Invoiced/Date Invoiced/Invoiced By/Status/Created, grouped Awaiting → Invoiced with running invoiced count; auth-exempt link handed to Krissy. Verified live via workbook readback (8 tasks, 3 invoiced) (2026-07-27). **Body column added 2026-07-30** (Krissy): task `bodyV2` rich-text notes now a visible column on the CRM Invoicing Report view (viewField `1a5ab455`, pos 4.5) AND in the workbook (markdown flattened, wrapped, before Created) — CRM side live immediately, export ships with the next ancapp deploy

- Weekly "Your Week in Focus" briefing engine live and delivery-verified (Jireh ask + Joe opt-in 2026-07-27; delivery repair + scheduler correction 2026-08-03): per-exec Sunday 4 PM ET digest — Outlook + documents + CRM timeline ranked into a personal Top 10 by the AI provider chain. `services/briefing/**`, POST `/api/briefing/weekly/run` (dryRun/scheduled/recipients; middleware+auth exempt), `FEATURES.WEEKLY_BRIEFING`. The host cron checks hourly and the route's New York-time guard permits only Sunday at 4 PM ET, making delivery independent of host timezone and DST. Pilot pages: astral-willow-h79s (Jireh) / regal-sparrow-46p6 (Joe) on here.now. The first scheduled send failed because the route was gitignored and recipient config was absent; direct Mail.ReadWrite inbox injection was then proven unreliable when Joe's mailbox moved both injected copies to Deleted Items. Delivery now uses the shared connected Microsoft Mail.Send path, records the message in Sent Items, and always BCCs Ahmad. Verified live: Joe's July 27–August 2 edition remains in Inbox from the connected ANC mailbox, the internal production smoke shows Ahmad in BCC, `/api/health` is ready, and the Sunday 4 PM ET schedule is active. Commit `c660a09b` (2026-08-03).
- Service-doc pagination fix (Natalia 2026-07-28, commit `7773d603`, VERIFIED live on 49ers Levi's Stadium contract `cms3arkuj00033398sgd4v6il`): pricing/manual/freeform tables + signature block carry INLINE `breakInside:"avoid"` (the Tailwind `break-inside-avoid` class alone was silently lost — the PDF renderer loads Tailwind from a CDN that can fail, so class-only print rules are unreliable in this pipeline; always pair with an inline style). Section headers get `breakAfter:"avoid"` so a heading can't strand at a page bottom. Puppeteer top margin 20px→36px in `generateProposalPdfServiceV2.ts` — Puppeteer applies ONE margin to every page, so this is the only lever for "page 2+ too close to the top". Verified by rendering the real PDF and measuring: 0.37in top margin on every continuation page, Description of Work + full table + SUBTOTAL together on p2 (2026-07-28)
- Service docs pass 4 (Natalia 2026-07-30 feedback pass, commit `c6d3290e`): (a) **Fifth Third Park service Excel now recognized** — the sheet labels contract-year columns with a bare year on the "Team:" row (`2026 | 2026 | 2026`), but `YEAR_LABEL_RE` only matched season ranges/"Year N", so detection returned null and the workbook fell to the LED column mapper. Bare years now match (bounded 1990-2100); repeated headers are numbered across the file's own term range with a disclosed warning; "Option 1" on the Team: row no longer becomes the client name; `listServiceSheets()` + `parseServiceSheet(wb, file, sheetName)` expose/import each priced option tab; import warnings render in the Service Proposal setup panel. (b) **Short Form Agreement (LOI) intro** defaults to her Letter of Intent paragraph verbatim via new `lib/loiIntro.ts` (segments → JSX + jsreport, same contract as `changeOrderIntro.ts`); LOI mode only, Short Form Contract untouched. (c) **SERVICE_PROPOSAL gained Scope of Services + Notes** — scope derived from the priced service lines per Alexis ("scope of work is just based on what these line items are"), notes ANC-authored; both toggleable/editable, hidden when empty. Verified by rendering real PDFs (LOI paragraph correct; Fifth Third Park → 2026/2027/2028 columns, "Included" mirrored verbatim, scope list, notes block). **STILL OPEN — Service Estimator asks from the same call recording** (flat-rate "type my number" override on every line, customizable line-item headings, parts-warranty input, options/alternates in one estimate, "Operating Expenses" heading at the internal/client boundary in the Excel export): see [project_service_estimator_natalia_alexis_asks.md] (2026-07-30)
- General Terms exhibit + Word round-trip (Natalia 2026-08-20, commits `1e988c38` / `9efed402` / `dcc84afe` / `847a46c4`, DEPLOYED + verified live): (a) **Labor and Materials warranties carry separate terms** — two fields on the T&C panel (`tcLaborWarrantyYears` / `tcMaterialsWarrantyYears`); either unset falls back to the shared `tcWarrantyYears`, so pre-split contracts render byte-identically. **Root cause found while wiring it: NONE of the General Terms fields were persisted** — `buildDocumentConfig` is the one list of details written into the `documentConfig` column and the warranty period, the four section toggles and `generalTermsBodyOverride` were all missing from it, so every setting reverted on reload. All now round-trip. Verified on production save → reload → render: labor 1 year, materials 10 years. (b) **The exhibit closes on one page.** Measured, not guessed: exhibit + document footer came to 976px of a 980px page — 4px. Tightened to 908px against a calibrated 924px budget. GOTCHA: the PDF shell sets `p,div{line-height:1.3!important}`, so an inline `lineHeight` never lands — `.leading-snug` (1.25, also `!important` in that shell) is the lever. Each clause also carries `break-inside:avoid` INLINE (the Tailwind class alone is dropped when the CDN fails — same lesson as 2026-07-28). (c) **A blank trailing page could hit ANY document**: `ProposalLayout`'s wrapper carries `p-3 sm:p-6`, and the 24px below the last element opened a further page whenever a document ended near the bottom. The container now carries an `anc-doc-page` handle and `generateProposalPdfServiceV2` zeroes its bottom padding — PDF only, in-app preview unchanged. Her Lincoln Financial Field contract: 10 pages → 9. (d) **Word round-trip keeps bullets and table order.** Two distinct causes: tables-at-the-end came from the assistant hand-rolling docx→markdown per request, and the missing bullets were ours — her document marks lists with the **ListBullet STYLE and carries zero `<w:numPr>`**, which the reader did not recognise, so every bullet in every uploaded document had been arriving as flat prose since attachment reading shipped 2026-08-11. New `POST /api/render/docx-markdown` (engine `lib/docx/docxToMarkdown.ts`, 10 tests) is the house converter, paired with the existing `markdown-docx` renderer; the same fix shipped inside the CRM as live image `anc/twenty-v2170-anc-docx-bullets:20260820`. Verified by running her real chat with her real Ravens document and opening the result: **0 bullets → 35**, all 4 tables in position, `M&T` intact. Regression guard: `PDF_PROBE=1 npx vitest run lib/serviceContracts/generalTermsPagination.test.ts` renders the exhibit at real page geometry and reports its headroom (`PDF_PROBE_SHOT=<dir>` also writes a PNG) (2026-08-20)
- Proposal Text Editor rich formatting (Natalia 2026-07-31): the actual Step 4 Text Editor now exposes Bold / Bulleted List / Numbered List controls for Introduction, Payment Terms, Additional Notes, and Signature Legal Text; the earlier LOI narrative editor uses the same controls for its legal paragraph and Scope of Work. Preview/PDF rendering now honors lists and emphasis in intro, payment, notes, scope, and signature blocks while legacy plain text keeps its existing line breaks/layout. Focused UI/PDF regressions: 65 passing; production build passes. RFP-protected/shared files unchanged.
- CRM user activity report live (Jireh 2026-08-10): `GET /api/render/user-activity-report-xlsx` renders a branded workbook of daily/weekly/monthly active users plus each member's last activity and last action; `?format=json` for agents. Never scans the ~933k-row timeline — reads the roster once, then each member's single most recent event, which yields last-activity AND the window counts. Counts reconcile exactly with a direct SQL aggregate (6/8/16 of 33 at ship). "Active" = created or changed something; the CRM keeps no login/page-view log, and the workbook says so on its face. Engine `services/reports/userActivity.ts`, 16 tests on the 24h/7d boundaries, future-skewed timestamps, unparseable dates (2026-08-10)
- RFP bid-form unicode fix (Jireh 2026-08-10, frozen-zone): HTTP headers must be ByteStrings, so a screen name containing an en dash (U+2013) threw and killed the whole fill. Jireh's `260720_DIGITAL_ASSETS_REVIEW.pdf` has 8 such names ("Level 400 – Grand Concourse"), which is why it died on that package specifically. Match metadata is now URI-encoded behind `X-Bid-Form-Metadata-Encoding: uri-v1` (`services/rfp/pipeline/bidFormResponseMetadata.ts`). NOTE: his costing workbook is a wide Digital Asset List with `Pixel Pitch/Resolution` at Z7; the filler detects blocks by `PIXEL PITCH` in **column A**, so 0 fills is correct behavior for that file, not a bug (2026-08-10)
- Word export action in the CRM (Jireh 2026-07-30 ask, shipped 2026-08-10): the markdown→docx formatter already existed at `/api/render/markdown-docx`; what was missing was a visible action. "Export to Word" is now a GLOBAL command-menu item in ANC Exports v0.1.8 opening a paste-and-download panel (`anc-apps/anc-exports-hub/src/front-components/answer-to-word.tsx`). The render endpoint allowlists `crm.ancsports.net` as a browser origin and exposes Content-Disposition, so the component POSTs and saves via a blob URL (front components cannot call `window.open`). Exports-hub tiles that point at a render endpoint now **probe it on mount** and only render when it answers — the app and the render service deploy separately (2026-08-10)
- M&S grid + team normalization (Jireh 2026-08-10): internal Name column hidden on both grids that still led with it, Verification Grid now leads with Game Date. All 1,118 M&S records carry a well-formed name — `scripts/backfill-mns-names.py` composition fixed after it emitted `× Red Sox (MLB 2026)` and a bare `(HP Full #1)` for records missing a sponsor or date. Duplicate team accounts merged: 49ers (→11 opps) and Florida Panthers (→16 opps, 47 people); the `Team | Venue` convention is the house standard across 32 team accounts and was kept canonical (2026-08-10)
- CRM AI file attachments fixed (Natalia 2026-08-11): attaching a document to a chat used to kill the whole conversation — the raw file went to the model as a `file` content part and the openai-compatible gateways rejected it (`Input should be a valid string, field: 'messages[N].content.str'`), 8 dead threads in 24h. Every attachment is now read to TEXT before the request leaves (`ancInlineAttachmentText` patched in after `convertToModelMessages`), so it works on EVERY model rather than only multimodal ones, and the AI actually reads the document instead of silently ignoring it. Extraction via Kreuzberg at `http://kreuz:8000` (reachable from the CRM containers; images/scans need `config={"ocr":{"backend":"paddle-ocr"}}` — the default tesseract backend is not registered). Source `/root/anc-crm-work/20260811-attachment-text/`, live image `anc/twenty-v2170-anc-attachment-text:20260811`, shipped through the safe-update pipeline. Verified live by attaching a PDF with a unique word and getting it back from a text-only model. Also fixed the update script itself: its boot-wait grepped the whole prod log each poll, which outran the guard's 900s window and made the guard revert a healthy cutover (2026-08-11)
- CRM AI exports wear one house template (Natalia 2026-08-11): the M&T Bank Stadium recap she sent is now the global look for every Word export — wordmark + title + date header band, blue caps section headings over navy sub-headings, justified body, navy-header table with content-proportional column widths, `www.anc.com` + "Page N of M" footer, ANC Sports Enterprises sign-off. Tokens in `lib/docx/ancDocumentTemplate.ts`. Also fixed: answers opening with prose lost their first sentence, and tables were split into even columns. Commit `87f3a1f7` (2026-08-11)
- AI Exports request/outcome audit trail (Ahmad 2026-08-20): app `anc-ai-exports` v0.3.2 now replaces the truncated chat-title-only context with the actual request preview plus a full side panel showing the exact user request, attached filenames, final assistant response, and download. The mirror sync joins files to both messages by the same `turnId` and backfilled all 218 records (182 with request text, 176 with outcomes; untraceable legacy rows say so instead of guessing). Live route `/page/0c5d1538-2a7d-4547-9b5c-fe2628ebef71`; production GraphQL + authenticated browser verified on Jireh's `ANC_NBA_Courtside_Registry.xlsx`; screenshot `/root/rag2/tmp/screenshots/ai-exports-request-outcome-live.png` (2026-08-20)
- House template pass 2 — Natalia's measured spec (2026-08-11, same evening): she read an export against her recap and rejected the type. The first pass set Calibri one size up, reasoning DejaVu is not installed on an ANC desk; she was explicit — "font is DejaVu Sans family ... main text 9 font, headers 10". Now literal: **DejaVu Sans / DejaVu Sans Mono, 9pt body, 10pt headings, 7pt date**, headings justified like the reference. Three more: the line under the title is **always the date** (the CRM's export button sent `subtitle: "Prepared from ANC AI"` into that slot — the option is gone from the API, and the runtime at `/root/anc-crm-work/20260810-word-export/` no longer sends it); the page was **A4** because `docx` defaults there and only margins were set — now explicit US Letter 12240×15840; and a bold column heading like "League" broke to "Leagu / e" because the column floor was measured off the regular weight (CHAR 120→145). Verified by rendering a real export and measuring the XML (DejaVu in docDefaults, no theme fonts on the heading styles) plus reading the rendered page. 28 tests (2026-08-11)
- Client-facing docs spell out screen specs (Natalia 2026-08-12): a Margin Analysis tab labels the first line of every display block "LED Hardware", which says nothing to a client — she was retyping each one by hand (24 on the Cleveland/Huntington Bank Field proposal). Mirror import now writes it from the LED Cost Sheet the same workbook carries: `Team Tunnel LED - 9.97' x 29.53' - 1.25mm - QTY 1`. Engine `lib/pricing/displaySpecDescription.ts`, wired into `app/api/proposals/import-excel/route.ts` Case A only — the parser and the frozen `/api/rfp/analyze/excel` route are untouched. The mirrored label survives on `sourceDescription`; an ambiguous name match is skipped rather than guessed. Her document mixes "QTY n" with "Total n" on the two rows whose names enumerate several displays; the dominant "QTY n" is written everywhere and stays editable. 17 unit tests + a run of the real Cleveland workbook (17/17 display tables rewritten, dims/pitch/qty identical to her PDF) (2026-08-12)
- Slack bid alerts on email intake (Jireh 2026-08-12, BofA Stadium ERP3): the intake now posts every processed email to Slack — venue, project, bid due date with its verbatim quote, attachments, and a link to either the CRM opportunity it applied to or the review queue. Routing follows sales' own convention (`#sales-bank-of-america-stadium-carolina-panthers`), matching on every identifying venue word so "America First Field" can't land in the Bank of America channel; `EMAIL_CRM_SLACK_CHANNEL_MAP` overrides, `EMAIL_CRM_SLACK_CHANNEL` catches the rest. Engine `services/intake/bidAlertSlack.ts`, 22 tests, rendering verified live in Slack. **Root cause of the underlying silence: `ancestimation@anc.com` is a distribution list with no mailbox (Graph `ErrorInvalidUser`), and `deals@anc.com` was never added to it — so `deals@` has received exactly ONE email since launch (Jeremy's 2026-07-06 test) and the poller has logged `processed:0` ever since.** Two human steps to make it flow: add `deals@anc.com` to the `ancestimation@` distribution list, and `/invite @anc` into each private pursuit channel (2026-08-12)
- Account Pipeline quick-create (Jireh 2026-08-13): the filter-chip row on a Company page's **Account Pipeline** tab now ends with **+ New Opportunity**, opening an inline draft form in the widget (name / status / business unit / stage / amount / award date). The account is implicit, nothing is written until Create, and the table reloads with the new deal highlighted — no leaving the account page. Same version fixed two live bugs the form exposed: **award dates rendered a day early for every US reader** (the whole opportunity corpus stores midnight UTC; Pipeline + LTV tabs now format with `timeZone: 'UTC'`), and the widget's blue was `--t-accent-primary`, a pale tint, so white-on-blue was unreadable in light mode (now `--t-color-blue` + `--t-font-color-inverted`). App `anc-account-opportunities-table` v0.3.11, commit `a10e14e` in `bashhh89/anc-apps`. Verified live on the Chicago White Sox account in Light and Dark; test records soft-deleted. **App installs need no EasyPanel deploy.** Still open, needs a data-model decision: Twenty's native Award Date field renders in the viewer's timezone (`Mar 14, 2027 5:00 PM` for a Mar-15 value) — aligning it means changing the field type on ~6.7k records (2026-08-13)
- LG Alliance report pass 3 (Jireh 2026-08-18, `df689985` + `183ffcc5` + `6dae2ade`, DEPLOYED + verified live 2026-08-19): LG Margin removed from all three sheets, Alliance 8% moved beside the Technology Vendor PO it is a percentage of, and **Sponsorship Value is a live `=SUM()` over the row's fiscal-year columns** — tier bands and TOTAL sum the rows beneath them, so editing a year in Excel moves everything above it. Rows with a sponsorship total but no year phasing (Washington SOW #10, Fenway) keep the entered figure and say so in a cell note rather than being zeroed by the formula. CRM-side and live now: `lgNotes` relabelled **"Internal Notes"**, LG Tier gained a **"No Sponsorship"** option banded below Tier 3 (2026-08-18) **Pass 4 (`6dae2ade`, 2026-08-19): every LG Tier the CRM offers is now a section on the rollup, empty or not** — he asked for "Needs Review" and "Additional Technology Opportunities from Sponsorship" before tagging a single deal into either, and a band was only drawn once a deal landed in it, so he opened the export and found neither. "Not yet tiered" stays conditional (absence of a decision, not one of the choices). GOTCHA: an empty band cannot use the block formula — `SUM(H9:H8)` runs backwards in Excel and swallows the band's own row, so an empty section writes a hard zero. Verified on production: seven sections in his order, both new ones at 0 deals/$0, sheet total unmoved at $346.5m PO / 126 deals / $27.7m at 8% / $36.8m sponsorship

- Won and Future NFL Business report (Jireh 2026-08-18, `666c252d`, DEPLOYED + verified live): `GET /api/render/nfl-business-report-xlsx` — every NFL opportunity not lost or no-bid, By Team (won book + open pursuits, subtotals, biggest first) and Deal Detail. **Root cause it replaces: he built this in the AI chat and every figure came back 1000x high** — CRM currency is `*AmountMicros` and the assistant scaled it by hand, so a $9,000 Giants graphics package (`18dd4ee3`, #201433) read as $9,000,000; the same run also returned 139 of 163 deals. Conversion now happens once in `dollars()` and the query pages `orderBy: {id}` to exhaustion. Engine `services/reports/nflBusiness.ts` (14 tests): absent figures stay blank rather than becoming zero, negative margins carry through, `?format=json` for agents. Verified off the live URL: 163 deals / 24 teams / $313,841,039.16 revenue / $44,219,453.26 margin, reconciling to the cent with a direct SQL aggregate (2026-08-19)
- CRM file downloads carry their real name, and Word documents wear the house template (Natalia 2026-08-19, live image `anc/twenty-v2170-anc-file-names:20260819`, DEPLOYED + verified live): her *"export looks like weird number file, I need the CRM to apply branding"* was **two** separate defects. (a) **Filename** — Twenty serves `GET /file/:fileFolder/:id` with a bare `Content-Disposition: attachment` and no `filename`, so the browser falls back to the last URL segment — the file id. Her Ravens/M&T Samsung assessment saved as `02fd019d-cc54-44b9-92bc-31194c5f90db.docx`; the trail shows she retried six times (`…_Branded`, `Ravens_Branded_MATX`, `…_Fixed`) and got a UUID every time. The name was never lost — the code interpreter stores `<fileId>-<name>`. New `file/utils/anc-file-download-name.js` recovers it and emits both `filename=` and RFC 8187 `filename*=`, ASCII-folding the quoted form because a header value must be a ByteString (the same trap that killed an RFP bid-form fill on an en dash, 2026-08-10). Derived in `getFilePresignedUrlOrStream` so BOTH download routes benefit; inline types still serve `inline`. **This was never AI-specific — every file download in the CRM was saving as a UUID.** 10 tests. (b) **Branding** — the prompt already says "ALWAYS use the `generate-pdf-browserless` tool … DO NOT use the code interpreter" for PDFs, but Word had no equivalent, so Scout fell through to the stock `docx` skill — an upstream python-docx tutorial with no ANC styling in it — and invented Calibri + navy/orange to look helpful. Scout's prompt now carries a Word section beside the PDF one, and the ANC `docx` skill leads with it: POST markdown to `proposals.anc.com/api/render/markdown-docx`, save the bytes, python-docx for reading/editing uploads only. Proven in a live E2B sandbox: 200, DejaVu Sans, wordmark embedded, **Calibri absent**, US Letter. Rollback tables `core.agent_backup_20260819_word_house_template` / `core.skill_backup_20260819_word_house_template`. NOTE: the mechanism is verified end-to-end; that Scout *chooses* it is model behaviour and still wants one real chat check (2026-08-19)
- CRM attachments stopped losing characters (Natalia 2026-08-19 voice note, live image `anc/twenty-v2170-anc-docx-fidelity:20260820`): her real ask was the **upload round-trip** — put a Word document in, get the same document back on the ANC template — and verifying that exact path exposed a third, wider defect. **The document service (Kreuzberg 4.9.9) drops every XML-escaped character out of a .docx, in every output format it offers.** `"Johnson & Johnson … Cost < 5% and > 2%. AT&T"` arrived as `"Johnson  Johnson … Cost  5% and  2%. ATT"`, so **M&T Bank Stadium had been reaching the model as MT Bank Stadium on every document anyone uploaded since attachment reading shipped 2026-08-11**. Its text mode also flattens structure completely (0 headings, 0 tables on her 29k-char assessment), so anything rebuilt from it returns as one wall of prose. Fix: `.docx` is now read locally — `anc-docx-text.js` walks the OOXML with `preserveOrder` (zlib + fast-xml-parser, both already in the image, no new dependency) and emits markdown with headings, tables and emphasis intact; a failed read falls through to the service, and every non-Word format still goes there because OCR is what it is genuinely good at. The service call also now sends `output_format: "markdown"` instead of flat text. On her real file: **M&T preserved**, 17 headings, 41 table rows — matching the service's structure while keeping the characters it was eating. 9 tests incl. a document-order guard (a table must not sink below the prose that followed it) (2026-08-20)
- AI download links stopped arriving one character wrong (Jireh 2026-08-20, live image `anc/twenty-v2170-anc-file-link-reconcile:20260820`, DEPLOYED + verified live): his Panthers bowl rack-room MEP export 403'd on a link that was **529 characters, correctly shaped, unexpired, and signed by the live key** — one byte off. `a`→`I` at offset 313 turned `"iat"` into `"!at"`, which fails the ES256 signature, and `FileByIdGuard` answers with the exact same `Forbidden resource` 403 an expired link gives. **Root cause: the assistant does not pass the minted URL through — it RETYPES ~440 characters of base64 into its own Markdown answer**, and a model copying a random string that long occasionally miscopies. A second production case had a **60-character run duplicated** mid-token. Neither is repairable by inspecting the broken link, but the message carrying it also carries the tool part it came from, and that holds the authoritative URL — so the persistence normaliser (the 2026-08-17 stray-quote seam) now **reconciles** every agent-chat URL in a text part against the minted URLs in that same message's tool outputs, keyed by file id. A mistyped file id is recovered by unique-nearest-match within the message's own files, and reconciliation refuses to act when two candidates are close — handing a stakeholder the wrong document is worse than a 403. Audit of every link ever issued: **11 of 81 messages carried a mistyped link**, across April, May, July and August — long-standing, never diagnosed, because the URL looks perfect. All 11 repaired in place by `repair-persisted-links.js` (same reconciler, one code path; backup `core.agent_message_part_backup_20260820_file_links`); every repair changed the token only, never the file id. 15 tests with both real production failures as fixtures, run inside the image build. Verified live: Jireh's original link now downloads the correct document (DejaVu Sans, US Letter, wordmark, no Calibri), and a fresh export driven through the real Ask AI panel persisted a link **byte-identical** to the minted one and returned 200. NOT covered: a link clicked mid-stream, before the message is persisted (2026-08-20)

- Branded PDFs place themselves (Jireh 2026-08-21): *"something that looks more professional and that's embedded in the bottom right with the actual ANC logo"* — `/api/render/brand-pdf` no longer takes a corner on faith. It **rasterizes each sheet** (`lib/pdf/pageInk.ts` — unpdf + @napi-rs/canvas, summed-area table over the ink), finds the blank paper nearest the requested corner (`lib/pdf/brandPlacement.ts`), and stamps the real vector wordmark there: mark, brand-blue rule, `www.anc.com`, no white pill. On his OKC Thunder A3 drawing it lands in the title block's own logo panel above DRN BY — the sheet is not grown, not scaled, and every figure on it is untouched. A sheet with genuinely no blank paper still falls back to the added strip, which covers nothing. **`placement` default is now `auto`; `band` and `overlay` must be asked for by name.** Optional second opinion: `BRAND_PDF_VISION_API_KEY` turns on a vision judge (`lib/pdf/visionPlacement.ts`) that is shown the rendered sheet and a shortlist of *already-verified-blank* rectangles and picks one — taste from the model, safety from geometry; `decidedBy` on the result says which answered. Provider-agnostic (`PlacementJudge`); `BRAND_PDF_VISION=off` disables it; absent key = geometry only, and nothing degrades. **GOTCHAS, all found by measuring, not reasoning:** (a) pdf.js renders onto a *transparent* canvas — read naively every blank page is solid black ink, so the paper is painted white before rendering and the ink map composites over white; (b) the paper's tone is the lightest value covering ≥1.5% of the sheet, NOT a mid percentile — a dense drawing is 95% line work on white and its *median* reads as a dark page, which refuses to place anything; (c) straight-line distance from the corner puts the mark **in the middle of the drawing** (a spot 257pt left/301pt up beats the title block 48pt left/471pt up) — cost is Manhattan with the side axis weighted 1.5, because staying flush to the edge is what reads as branding; (d) laddering clearance and stopping at the first rung that works trades placement for breathing room and loses the same way — all clearances compete on one list; (e) stepping y on a grid steps *over* a panel only a few points taller than the mark, so blank vertical runs are read exactly, one integral query per row. **`gemini-2.5-flash` is retired** (404 that reads like a network error); default judge model is `gemini-3.6-flash`. 57 tests. `BRAND_PDF_PROBE=<file.pdf> npx vitest run lib/pdf/brandPdf.test.ts -t probe` brands a real document and writes it out to be looked at (2026-08-21)
- **Nano-banana / `nano-pdf` evaluated and deliberately NOT used for drawings (2026-08-21):** `pip install nano-pdf` (CLI at `/root/.local/bin/nano-pdf`, needs a **billed** `GEMINI_API_KEY` — the free tier returns 429 for image models). It renders each page, has Gemini 3 Pro Image **repaint** it, OCRs a text layer back on, and swaps the page. Run on the real Thunder sheet it put the logo exactly where Jireh asked **and rewrote the drawing**: resolution `13920px×2008px`→`12000px+2000px`, total weight `9976 Kg`→`5076 Kg`, `638 Panel`→`636`, `290 PCS`→`200`, size `182.677"`→`162.077"`, receiver card `A8S-N`→`ARB-N`. Every number, on a client submittal. Keep it for decks, one-pagers and marketing PDFs where repainting is fine; never point it at a dimensioned drawing, a spec sheet or anything a client will build from. The half of it that IS right — a model looking at the page and deciding — is what `visionPlacement.ts` uses, without the repaint (2026-08-21)
- **FLAGGED, not actioned:** 295 opportunities workspace-wide have no amount, close date, notes or tasks — **242 created on 2026-04-26 in one ~20-minute run** (the Georgetown three came from it). Clusters as repeats of the same name on the same account (NBA Arena ×15, Indiana Fever ×12, San Antonio Missions ×10). 4.3% of 6,827 opportunities, inflating the account-level counts Jireh reads daily. Needs Ahmad's go-ahead — bulk destructive (2026-08-10)
- **FLAGGED, needs a data-model decision:** the Nielsen/M&S flow cannot roll media revenue to accounts — `mediaPlacement.opportunityId` is empty on 918/918, there is no placement→contract relation, and `_sponsorTeamContract` has no `opportunityId`. It cannot be inferred: each sponsor carries many opportunities (Hankook 45, Tire Rack 9). Completing it needs new relations, which is a check-in, not a solo build (2026-08-10)
- Bid alerts stopped announcing invoices as bids (Jireh 2026-08-20, `e75ffc31`, PUSHED — deploy pending): the intake reads every message reaching the estimation inbox, and `postBidAlert` fired unconditionally on all four branches, so a Dyson & Womack invoice ("Re: Invoice #1587 for OBM") posted to a pursuit channel as a "New bid email" asking a human to confirm a deal against "University of Rhode Island Courtside and Stanchion 2025". The data path was already correct — it read the message as an invoice, tagged the date `kind:"other"`, scored 0.7 under the 0.75 threshold and wrote nothing to the CRM; only the alert was wrong. New `looksBidRelated()` gates inside `postBidAlert` itself (not at the call sites, so no future branch can bypass it): a verified `proposal_due` date, or a word-bounded bid word in the subject for an addendum quoting no new date; a failed read with no extraction judges on subject alone. Word boundaries matter — unanchored `/bid/` matches "forbidden" and `/sow/` matches "Wilson". 46 tests, his actual invoice as the fixture (2026-08-20)

## 🎯 ANC Twenty CRM — #1 priority (decision Mon-Tue 2026-04-21/22)
- Live at `https://crm.ancsports.net` — replaces Salesforce
- 6,812 live opportunities (verified 2026-07-17; the old 8,360 figure was the post-SF-match peak and is stale) · 3,730 companies · 5,160 revenue splits · 20K+ design requests
- 17 new Opp fields added 2026-04-17: probability, proposalDueDate, substantialCompletionDate, paidAmount, percentPaid, accountExecutive, accountExecutiveEmail, margin, revenue2026, margin2026, revenue2027, margin2027, proposalStage, priority, pricingComplete, technologyVendorPartner, pricingCompleteDate
- 11 new dashboard widgets on ANC 2026 Company Dashboard matching Jireh's SF layout
- 2 new views: Proposal Pipeline + Estimation & Proposals (Natalia's daily)
- Boyka custom agent with 30 ANC-specific skills
- **Full state in `/root/.claude/skills/crm-knowledge/SKILL.md`** — read this for any CRM question; update after any CRM change.
- **Read-only SF access:** `salesforce-crm` skill, creds at `/root/.sf-creds`

## 🛑 ABSOLUTE RULE: NO SHORTCUTS
- Build it RIGHT, not fast. Fix the ROOT CAUSE, not the symptom.
- NO band-aids, quick fixes, silent fallbacks, hardcoded values, or magic numbers.
- NO `sed`, regex surgery, or suppressing errors.
- Fix the code, don't change the test to match wrong behavior.
- *If about to take a shortcut, stop and say: "I was about to take a shortcut. Here's the real fix instead."*

## 📝 CLAUDE.md MAINTENANCE
- Update this file at the end of sessions with new rules, decisions, or status changes.
- Move fixed bugs to "resolved", add features to "What's Live" (1-liner).
- This is the single source of truth. Keep it tight. Never remove rules unless told.
