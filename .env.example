# =============================================================================
# ANC Proposal Engine – Environment Variables
# =============================================================================
# Copy this file to .env and fill in real values. Never commit .env or real keys.
# All values below are placeholders only.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL – Prisma)
# -----------------------------------------------------------------------------
# Primary connection string. Prisma also checks POSTGRES_*, PRISMA_* if unset.
DATABASE_URL=postgres://user:password@localhost:5432/dbname?sslmode=disable

# -----------------------------------------------------------------------------
# Serper (Google Search API – venue/stadium lookups)
# -----------------------------------------------------------------------------
# Get key at https://serper.dev – used when LLM is unavailable for address lookup.
SERPER_API_KEY=your-serper-api-key

# -----------------------------------------------------------------------------
# Z AI / Vision (e.g. GLM-4V for image analysis)
# -----------------------------------------------------------------------------
Z_AI_API_KEY=sk-placeholder-key
Z_AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
Z_AI_MODEL_NAME=glm-4v

# -----------------------------------------------------------------------------
# AnythingLLM – RAG / “Brain” API
# -----------------------------------------------------------------------------
# Base URL (no trailing slash); app will append /api/v1.
ANYTHING_LLM_URL=https://your-anythingllm-host.example.com
ANYTHING_LLM_KEY=your-anythingllm-api-key
ANYTHING_LLM_WORKSPACE=researcher
ANYTHING_LLM_MASTER_CATALOG_URL=https://your-anythingllm-host.example.com/api/v1/workspace/slug/documents
ANYTHING_LLM_MASTER_WORKSPACE=dashboard-vault

# -----------------------------------------------------------------------------
# PDF generation (Puppeteer / Browserless)
# -----------------------------------------------------------------------------
# Optional. If set, app connects to Browserless for PDF generation; else uses local Chromium.
BROWSERLESS_URL=wss://your-browserless-host.example.com?token=your-token
# Optional. Path to Chromium when not using Browserless (e.g. in Docker).
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# -----------------------------------------------------------------------------
# NextAuth (Auth.js v5) – required for session encryption
# Generate: openssl rand -base64 32  or  npx auth secret
AUTH_SECRET=your-auth-secret-at-least-32-chars

# Microsoft Entra ID SSO
# Callback URL: https://proposals.anc.com/api/auth/callback/microsoft-entra-id
AUTH_MICROSOFT_ENTRA_ID_ID=your-application-client-id
AUTH_MICROSOFT_ENTRA_ID_SECRET=your-client-secret-value
AUTH_MICROSOFT_ENTRA_ID_ISSUER=https://login.microsoftonline.com/your-tenant-id/v2.0/
AUTH_MICROSOFT_ALLOWED_DOMAINS=anc.com
NEXT_PUBLIC_MICROSOFT_SSO_ENABLED=false

# Sentry (error monitoring)
# -----------------------------------------------------------------------------
# DSN from Sentry project settings. App works without it (errors only in console).
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# For source map uploads in CI (optional).
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# -----------------------------------------------------------------------------
# App URL (public – used in links, redirects, PDF base href)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_BASE_URL=http://localhost:3003

# -----------------------------------------------------------------------------
# Google Search Console (optional – meta tag verification)
# -----------------------------------------------------------------------------
GOOGLE_SC_VERIFICATION=optional-verification-string

# -----------------------------------------------------------------------------
# Email (Nodemailer – sending proposal PDFs)
# -----------------------------------------------------------------------------
NODEMAILER_EMAIL=noreply@example.com
NODEMAILER_PW=your-app-password

# -----------------------------------------------------------------------------
# DocuSign (e-signatures)
# -----------------------------------------------------------------------------
DOCUSIGN_BASE_URL=https://demo.docusign.net
DOCUSIGN_INTEGRATOR_KEY=your-integrator-key
DOCUSIGN_USER_ID=your-user-id
DOCUSIGN_ACCOUNT_ID=your-account-id
DOCUSIGN_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
DOCUSIGN_WEBHOOK_SECRET=your-webhook-secret

# -----------------------------------------------------------------------------
# Salesforce (optional integration)
# -----------------------------------------------------------------------------
SALESFORCE_CLIENT_ID=your-client-id
SALESFORCE_CLIENT_SECRET=your-client-secret
SALESFORCE_INSTANCE_URL=https://your-instance.my.salesforce.com

# -----------------------------------------------------------------------------
# Settings encryption (stored credentials – use a strong random key in production)
# -----------------------------------------------------------------------------
SETTINGS_ENCRYPTION_KEY=generate-a-long-random-string-for-production

# -----------------------------------------------------------------------------
# Userback (feedback widget – optional; token is public, used in browser)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_USERBACK_ACCESS_TOKEN=your-userback-token

# -----------------------------------------------------------------------------
# Tuning (optional – defaults in code if unset)
# -----------------------------------------------------------------------------
# NATALIA_MARGIN_THRESHOLD=0.2
# ANC_PRODUCT_CONFIDENCE_THRESHOLD=0.85

# -----------------------------------------------------------------------------
# Scripts / CI (optional)
# -----------------------------------------------------------------------------
# API_URL=http://localhost:3003
# DEPLOYED_APP_URL=https://your-app.easypanel.host
# DEFAULT_EXCEL_PATH=/path/to/default.xlsx
# EXCEL_PATH=/path/to/excel.xlsx
# CLIENT_NAME=Demo Client
# MOCK_MODE=false
# ANALYZE=false
# CI=
